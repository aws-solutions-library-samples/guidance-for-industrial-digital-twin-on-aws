apiVersion: 1

providers:
  - name: BreweryProvider # A uniquely identifiable name for the provider
    folder: Brewery Manufacturing # The folder where to place the dashboards
    type: file
    options:
      path: /var/lib/grafana/dashboards